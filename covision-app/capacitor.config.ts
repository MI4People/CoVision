import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.covision',
  appName: 'covision-app',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
